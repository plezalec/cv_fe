import { BuildHtmlFromJsonPipe } from './build-html-from-json-pipe';

describe('BuildHtmlFromJsonPipe', () => {
  it('create an instance', () => {
    const pipe = new BuildHtmlFromJsonPipe();
    expect(pipe).toBeTruthy();
  });
});
